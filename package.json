{
  "name": "quill",
  "version": "0.10.0",
  "description": "",
  "main": "assets/js/dist/index.js",
  "scripts": {
    "build": "rm -rf public && source bin/activate && statik && cp now.json public/now.json",
    "serve": "http-server -p 8000",
    "setup": "npm i && npm run build && npm run serve",
    "deploy": "npm run build && now public && now alias"
  },
  "repository": {
    "type": "git",
    "url": "https://bitbucket.org/sigstr/quill/src/master/"
  },
  "author": "Sigstr Inc.",
  "devDependencies": {
    "@fortawesome/fontawesome-pro": "^5.6.3",
    "babel-core": "^6.26.3",
    "babel-loader": "^8.0.5",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^10.0.0",
    "browserify": "^16.2.3",
    "gulp": "^4.0.0",
    "gulp-connect": "^5.7.0",
    "vinyl-source-stream": "^2.0.0"
  },
  "dependencies": {
    "get-contrast-ratio": "^0.1.4"
  }
}