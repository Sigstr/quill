name: CI BUild to gh pages

on:
  push:
    branches:
    - master

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to GitHub Pages
      uses: JamesIves/github-pages-deploy-action@2.0.0

